extends base
include mixins/video

block content 
    form(method="GET").search__form 
        input(type="text", name="keyword", placeholder="Search by Title")
        button 🔍

    div.video-grid
        each video in videos
            +video(video)
        else
            p.empty__message Oh no, search again!